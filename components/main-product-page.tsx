import { title } from "@/components/primitives";

export const MainProductPage = () => {
  
    return (
        <section id="product" className="py-20">
            <h1 className={title()}>Product and Services</h1>
        </section>
    );
};